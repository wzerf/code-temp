- Communicates in Simplified Chinese and expects assistant responses in Chinese. Confidence: 0.9
- Verifies delivered code hands-on by running the full stack locally (React dev server + Java backend with MySQL) and reports runtime errors verbatim (stack traces/logs) expecting in-place fixes — not just "compile-level" acceptance. Confidence: 0.95
- Runs `git status` before `git push` to verify changes. Confidence: 0.7
- Prefers adopting the official vendor SDK (latest version) over hand-written protocol/HTTP code — explicitly asked to replace the hand-rolled OkHttp JSON-RPC MCP client with the official modelcontextprotocol/java-sdk. Confidence: 0.9
- Enforces strict server-side security red lines on any external/integration feature — e.g., only HTTPS Git URLs, no user-info in URLs, DNS-resolution check rejecting loopback/private/link-local addresses, file count/size limits on scanned content, secrets encrypted at rest and held only in memory. The assistant's service code and tests for the Git source import followed this pattern and the user accepted it without comment. Confidence: 0.75
- Keeps real credentials out of the repository — temporary smoke tests using real API keys are run and then deleted before commit; the assistant flagged this and the user did not object. Confidence: 0.7
- Keeps derived schema/seed artifacts in sync — when V4 seed migrations were extended, the prod copy (migration-prod) and the schema.sql header statistics were updated in the same change. Confidence: 0.7
- Breaks large multi-step feature work into an explicit task list (todo_write) and drives it to completion before summarizing. Confidence: 0.7
- Prefers admin list/table views to surface quantitative metadata inline (e.g., per-skill resource file count as a column) and expects such columns/capabilities to be applied consistently across all related module tables (preview, draft, release, market), not just one view — explicitly asked "其他几个模块的也在 table 显示一下". Confidence: 0.6
- Wants records in management lists grouped by provenance/source so related items stay together (e.g., Skill drafts synced from the same Git source shown under one group, manually created ones labeled separately). Confidence: 0.5
- Cares that edit/view echo-back (回显) in admin UI is complete rather than crude — opening an edit or detail view should surface the full record including read-only metadata (status, review comment, source/provenance, content hash, timestamps, resource count), not just editable fields; non-editable lifecycle states should open read-only instead of presenting a form the backend will reject; and expects such echo/UX work to be applied uniformly across sibling tabs (草稿/Release/市场), not just one view. Also actively QA-reports presentation defects in the running UI and expects fixes: a field shown twice (e.g., visibility in both the metadata area and the form) and rows that exist but render with visibly empty content (layout squeezing long text) are treated as bugs to fix in place. Re-checks in the running app after a fix and pushes back when a symptom persists (e.g., "资源文件回显还是空的，但数据库是有数据的"), expecting the true root cause to be traced end-to-end (DB → backend API → frontend rendering) rather than accepted as a cosmetic/layout workaround. Confidence: 0.8
- Prefers UI layout/positioning of shared components (e.g., tab bars) to be consistent across sibling admin pages, using an existing page as the reference implementation to align against (e.g., asked to align MCP/Skill tab bars to the log audit page). Confidence: 0.6
- Works in a Windows dev environment: shell commands run via PowerShell, Unix pipe utilities like `head` are unavailable (use PowerShell `Select-Object -First N` instead), and command `cd` is relative so passing the tool's cwd parameter is more reliable. Confidence: 0.7
- Uses pnpm as the package manager; run project tooling via `pnpm exec ...` (e.g., `pnpm exec tsc`) rather than bare `npx`/`npm`. Confidence: 0.7
- When making frontend edits (React/TS), verifies by running the TypeScript type check before reporting completion. Confidence: 0.7
- Prefers sibling admin features to share the same interaction pattern, not just visual layout — e.g., asked that Skill binding be changed to match MCP binding (explicit bind button + auto-loaded options) instead of a divergent auto-bind-on-select flow. Confidence: 0.7
